apiVersion: v1
kind: Service
metadata:
  name: nats
  namespace: celery-perf
  labels:
    app.kubernetes.io/name: nats
    app.kubernetes.io/part-of: celery-perf
spec:
  selector:
    app.kubernetes.io/name: nats
  ports:
    - name: client
      port: 4222
      targetPort: 4222
    - name: monitoring
      port: 8222
      targetPort: 8222
